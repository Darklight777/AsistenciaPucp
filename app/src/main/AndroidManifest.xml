<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="pe.edu.pucp.grupo02.asistenciapucp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".features.teacher.token.TeacherTokenActivity"></activity>
        <activity android:name=".features.teacher.messages.TeacherMessagesActivity" />
        <activity android:name=".features.teacher.attendance.TeacherAttendanceActivity" />
        <activity android:name=".features.student.token.StudentTokenActivity" />
        <activity android:name=".features.student.messages.StudentMessagesActivity" />
        <activity android:name=".features.student.attendance.StudentAttendanceActivity" />
        <activity android:name=".features.teacher.TeacherActivity" />
        <activity android:name=".features.student.StudentActivity" />
        <activity android:name=".features.home.HomeActivity" />
        <activity android:name=".features.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>